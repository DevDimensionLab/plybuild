
  # After registering a name on build.snapcraft.io, commit an uncommented line:
  name: co-pilot-cli
  version: 'v0.2.24' # just for humans, typically '1.2+git' or '1.3.2'
  summary: Co-pilot is a developer tool for automating common tasks on a spring boot project
  description: |
    Co-pilot is a developer tool for automating common tasks on a spring boot project

  grade: stable # must be 'stable' to release into candidate/stable channels
  confinement: devmode # use 'strict' once you have the right plugs and slots

  parts:
    my-part:
      # See 'snapcraft plugins'
      plugin: nil
  
